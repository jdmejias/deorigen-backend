
import { useTranslations } from "next-intl";
import { Button } from "@/components/ui/button";
import { Link } from "@/i18n/routing";
import { Progress } from "@/components/ui/progress";
import { TrendingUp, Users, Calendar, Target } from "lucide-react";
import Image from "next/image";

export default function InvestmentPage() {
  const t = useTranslations("Investment");

  const projects = [
    {
      id: 1,
      title: "Tanques de Almacenamiento Huila",
      farmer: "Asociación CooHuila",
      goal: "$15,000",
      raised: 75,
      investors: 42,
      deadline: "24 días",
      imageSeed: "inv1",
    },
    {
      id: 2,
      title: "Paneles Solares para Secado",
      farmer: "Familia Rodriguez",
      goal: "$8,500",
      raised: 45,
      investors: 18,
      deadline: "12 días",
      imageSeed: "inv2",
    },
    {
      id: 3,
      title: "Modernización de Beneficiadero",
      farmer: "Cacaoteros del Sur",
      goal: "$22,000",
      raised: 20,
      investors: 9,
      deadline: "45 días",
      imageSeed: "inv3",
    },
  ];

  return (
    <div className="min-h-screen bg-surfaceSoft/30 py-24">
      <div className="container mx-auto px-4">
        <div className="text-center max-w-3xl mx-auto mb-20">
          <h1 className="text-4xl lg:text-5xl font-bold font-headline text-textPrimary mb-6">{t("title")}</h1>
          <p className="text-lg text-textSecondary">{t("subtitle")}</p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {projects.map((project) => (
            <div key={project.id} className="bg-white rounded-[32px] overflow-hidden shadow-card border border-[#E7DED6] hover:lift transition-all">
              <div className="relative h-64 overflow-hidden">
                <Image 
                  src={`https://picsum.photos/seed/${project.imageSeed}/600/400`}
                  alt={project.title}
                  fill
                  className="object-cover"
                />
                <div className="absolute top-6 left-6">
                  <span className="bg-primary text-white px-4 py-1.5 rounded-full text-xs font-bold shadow-soft">
                    Active
                  </span>
                </div>
              </div>
              <div className="p-8">
                <h3 className="text-2xl font-bold font-headline text-textPrimary mb-2">{project.title}</h3>
                <p className="text-textSecondary text-sm mb-8 flex items-center gap-1">
                  <Users className="w-4 h-4" />
                  {project.farmer}
                </p>

                <div className="space-y-4 mb-8">
                  <div className="flex justify-between items-end">
                    <span className="text-sm font-bold text-textPrimary">{t("goal")}: {project.goal}</span>
                    <span className="text-primary font-bold">{project.raised}%</span>
                  </div>
                  <Progress value={project.raised} className="h-2.5 bg-surfaceSoft" />
                </div>

                <div className="grid grid-cols-2 gap-4 mb-8">
                  <div className="bg-surfaceSoft/50 p-4 rounded-[16px]">
                    <p className="text-[10px] text-textSecondary uppercase font-bold tracking-wider mb-1">Investors</p>
                    <p className="text-sm font-bold text-textPrimary">{project.investors}</p>
                  </div>
                  <div className="bg-surfaceSoft/50 p-4 rounded-[16px]">
                    <p className="text-[10px] text-textSecondary uppercase font-bold tracking-wider mb-1">Time Left</p>
                    <p className="text-sm font-bold text-textPrimary">{project.deadline}</p>
                  </div>
                </div>

                <Button asChild className="w-full primary-gradient text-white rounded-[16px] h-14 font-bold overflow-hidden">
                  <Link href="/contacto">{t("invest")}</Link>
                </Button>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

